spring.application.name=ecommerce
server.port=${SERVER_PORT:2021}

# Database Configuration
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:vivekanand}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Qawsedrftg@321}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Max file size (e.g., 100MB)
spring.servlet.multipart.max-file-size=100MB
# Max request size (should be >= max-file-size)
spring.servlet.multipart.max-request-size=100MB


# JPA Configuration
spring.jpa.hibernate.ddl-auto=${DB_DDL_AUTO:update}
spring.jpa.show-sql=${DB_SHOW_SQL:true}

file.upload-dir=uploads/
spring.web.resources.static-locations=classpath:/static/,file:uploads/




